['it looks like a good start! after looking at the pr. i think it might be good to include something that actually uses the parsed docs somehow. something like getting the argument names from the doc if it exists and falling back on the current method. does that make sense?also. a couple little fixes:- [x] the tests are failing- [x] the pr should be given a better name- [x] i would check out the codeclimate issues']