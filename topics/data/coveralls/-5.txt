["added github/travis coverage hooks. for when we have istanbul integrated. if we want to test this with the private repo. will need additional `.coveralls.yml` config. otherwise we're good to go. once we start generating the lcov report from istanbul.", 'michaelbpaulson trxcllnt - merging this.', 'uncommented coveralls coverage submission', 'i am doing the merge though the coveralls failed.  i only moved files so i am slightly confused why this failed.']