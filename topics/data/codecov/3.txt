["not sure why codecov is saying coverage decreased. didn't touch any source code.", 'count babel tests towards babylon code coverage. as we\'re already running babel\'s tests in ci to detect any integration issues early. we can also use them to meaningfully bump up babylon\'s test coverage. by doing this with an instrumented build of babylon and using codecov\'s automatic report merging feature.in practice this currently helps us cover things like `do` expressions and the `::` operator which are conspicuously uncovered in the current `master`. obviously for new features the ideal still is to add covering tests from the get-go.(also. file this under "workarounds we can retire if babylon is ever merged back into the babel monorepo" )', "setup codecov. current setup of codecov.io isn't working ..."]